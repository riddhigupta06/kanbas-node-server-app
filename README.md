# Running the app

To get all the dependencies, run:
`npm install`

To start the server, run:
`npm start`

To start the server using nodemon, run:
`npm run nodemon`

# Deploying
The default `PORT` is `4000` if none are specified in the environment.

Set the build command as:
`npm install`

Set the start command as:
`npm start`